from setuptools import setup, find_packages

version = '0.1.0'
author = 'Jay Raja'
description = 'Sample Kafka Project'

setup(
    name = 'src',
    version = version,
    author = author,
    description = description,
    packages = find_packages(),
)